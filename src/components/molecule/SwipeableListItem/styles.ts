import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {
    overflow: "hidden",
  },

  rightAction: {
    position: "absolute",
    right: 0,
    top: 0,
    bottom: 0,
  },

  gradient: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
