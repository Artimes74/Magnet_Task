import { StyleSheet } from 'react-native';
import { padding } from 'theme/primitive';

const styles = StyleSheet.create({
  pager: {
    flex: 1,
    paddingTop: padding.x3,
  },

  page: {
    flex: 1,
  },
});

export default styles;
